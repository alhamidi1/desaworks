/**
 * Next.js configuration (JavaScript) — generated from existing next.config.ts
 * Next 16 requires a JS config file; keep next.config.ts for reference if desired.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep this in sync with next.config.ts if you add options there.
};

module.exports = nextConfig;
